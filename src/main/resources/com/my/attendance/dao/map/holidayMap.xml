<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN' 
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='com.my.attendance.dao.map.HolidayMap'>
	<select id='selectHolidays' resultType='holiday'>
		select hol_no, hol_date, hol_content
		from holidays
		order by hol_date
	</select>
	
	<insert id='insertHoliday'>
		insert into holidays (hol_no, hol_date, hol_content, emp_no) 
		values (attendance.holiday_holidayno_seq.nextval, #{holidayDate} ,#{holidayContent}, 1001)
	</insert>
	
	<update id='updateHoliday'>
		update holidays
		set hol_date = #{holidayDate}, hol_content = #{holidayContent}
		where hol_no = #{holidayNo}
	</update>
	
	<delete id='deleteHoliday'>
		delete holidays
		where hol_no = #{holidayNo}
	</delete>
</mapper>	