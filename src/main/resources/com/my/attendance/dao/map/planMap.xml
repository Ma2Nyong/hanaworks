<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN' 
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='com.my.attendance.dao.map.PlanMap'>
	<select id='selectPlans' resultType='plan'>
		select plan_title, plan_date , plan_content
		from plans
		order by plan_id
	</select>
	
	<insert id='insertPlan'>
		insert into plans (plan_id, plan_title, plan_date, plan_content) 
		values (attendance.plan_planid_seq.nextval, #{planTitle}, #{planDate} ,#{planContent})
	</insert>
	
	<update id='updatePlan'>
		update plans
		set plan_title = #{planTitle}, plan_date = #{planDate}, plan_content = #{planContent}
		where plan_id = #{planId}
	</update>
	
	<delete id='deletePlan'>
		delete plans
		where plan_id = #{planId}
	</delete>
</mapper>	